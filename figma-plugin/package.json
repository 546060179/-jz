{
  "name": "figma-fade-animation-plugin",
  "version": "1.0.0",
  "description": "Figma 插件：选中元素后配置动效参数，自动生成 React/Android/iOS 三端代码",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.98.0",
    "typescript": "^5.4.0"
  }
}
